# RevisionBase Image Slots

Create these branded images when you are ready. The site will crop images with `object-fit: cover`, so keep important text, logos, faces, and UI details away from the edges.

## Homepage

- `home-hero.jpg` - full-width hero background. No locked ratio because it fills the viewport and crops responsively; make it at least `2400x1400`, with the main subject centred.
- `home-workflow.jpg` - homepage gallery card. Locked to `4:3`; recommended export `1600x1200`.
- `home-dashboard.jpg` - homepage gallery card. Locked to `4:3`; recommended export `1600x1200`.
- `home-paper-card.jpg` - homepage gallery card. Locked to `4:3`; recommended export `1600x1200`.

## Other Pages

- `papers-finder.jpg` - A-Level past-paper finder page. Locked to `16:10`; recommended export `1600x1000`.
- `toolkit-hero.jpg` - toolkit landing page. Locked to `16:7`; recommended export `1920x840`.
- `resources-vault.jpg` - resource database hero. Locked to `5:4`; recommended export `1500x1200`.
- `subjects-map.jpg` - subject hub. Locked to `16:7`; recommended export `1920x840`.
- `blog-journal.jpg` - blog/journal page. Locked to `16:7`; recommended export `1920x840`.

The pages have styled empty states for these names, so the layout already works before the final images are added.
